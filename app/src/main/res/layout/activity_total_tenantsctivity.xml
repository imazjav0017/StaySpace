<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_total_tenantsctivity"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/totalStudentsList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></android.support.v7.widget.RecyclerView>

    <TextView
        android:id="@+id/noTenantsText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="No Tenants Available
Get Started By Adding Rooms and Checking In"
        android:textSize="25sp"
        android:textStyle="bold"
        android:visibility="invisible" />

    <CheckBox
        android:layout_width="wrap_content"
        android:background="@color/green"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:id="@+id/roomsOnlySearchFilter"
        android:visibility="invisible"
        android:padding="5dp"
        android:text="Include Only Students In This Room?" />
</RelativeLayout>
